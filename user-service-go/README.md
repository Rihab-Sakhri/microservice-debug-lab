# User Service (Go)

Go microservice exposing a /users endpoint.

## Features
- Returns list of users in JSON
- Intentional bug for debugging practice (?bug=true)
- Logging included

## Run locally
```bash
go run main.go